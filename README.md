# Maze-DFS-solver-GUI
This project implements a maze solver using the Depth First Search (DFS) algorithm with a C# Windows Forms interface. It visualizes the solving process step-by-step and allows users to interact with and modify the maze.
